use std::collections::HashMap;

pub struct Template {
    text: String,
}

impl Template {
    fn from_blob() -> Template {
        unimplemented!();
    }

    fn render(params: HashMap<String, String>) {
        unimplemented!();
    }
}
