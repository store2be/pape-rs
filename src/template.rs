use std::collections::HashMap;

pub struct Template {
    text: String,
}

impl Template {
    fn from_blob(bytes: Vec<u8>) -> Template {
        unimplemented!();
    }

    fn render(params: HashMap<String, String>) {
        unimplemented!();
    }
}
